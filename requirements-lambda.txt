# Minimal, wheel-only deps for Lambda image
numpy==1.26.4
# Force CPU-only PyTorch wheel
torch==2.4.1+cpu
transformers==4.44.2
tokenizers
huggingface-hub
safetensors
sentencepiece==0.2.0
fastapi==0.116.1
mangum==0.19.0
tqdm
